# content-vault

Phase 1 foundation for a local personal content management tool for family travel media.

## Run

```bash
npm install
npm run dev
```

The Express app runs on `http://localhost:3000`.
